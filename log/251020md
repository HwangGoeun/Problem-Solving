# 2727. Is Object Empty

객체 또는 배열이 인수로 전달되었을 때, 전달된 값이 비었는지 여부를 반환하는 문제입니다.

```js
var isEmpty = function(obj) {
    return (JSON.stringify(obj).length === 2);
};
```

문제에 `You may assume the object or array is the output of JSON.parse.` 와 같은 문장이 있길래, 잔머리를 굴려 `JSON.stringify()`를 이용해 문제를 풀긴 했습니다만... 객체 그 자체를 비교한 것은 아니기 때문에 더 좋은 방법이 있을 것이라 생각했습니다.

<br>

### 오류 점검하기

기존에는 `obj.length === 0` 과 같은 식으로 문제를 풀려고 했습니다만, 빈 객체의 경우 반환값이 `undefined`가 나와 해결할 수 없었습니다.

<br>

### 다른 코드와 비교하기

```js
var isEmpty = function(obj) {
    return !(Object.keys(obj).length);
};
```

`Object.keys()` 메서드를 이용한 코드입니다.

객체의 키가 있는지 여부를 확인하는 코드를 작성하고 싶었는데, 때마침 관련 메소드가 있었습니다!

`Object.keys()` 메소드는 객체의 키 이름을 배열의 형태로 반환해주는데, 이를 활용해 반환된 배열의 길이로 객체의 공백 여부를 확인할 수 있었습니다.

배열의 길이가 0이면 `!` 연산자를 이용해 `true`를, 아니면 그 반대 결과를 반환할 수 있도록 했습니다.
